# Analysis

This folder contains scripts for analyzing benchmark results.

## Requirements

Install dependencies:

`pip install -r requirements.txt`

## Run

Analyze results and generate summary + plots:

`python analyze_results.py --input ../results/benchmark-results.csv --plots plots`

The summary is written to `summary.csv` and plots are saved under `plots/`.
